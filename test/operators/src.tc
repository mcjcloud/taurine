var (int) a = 3 + 3;
etch a; // 6
a += 3;
etch a; // 9

var (int) b = 3 * 3;
etch b; // 9
b *= 3;
etch b; // 27

var (num) c = 3.0 / 3.0;
etch c; // 1.000000
c /= 3;
etch c; // 0.333333

var (int) d = 3 - 3;
etch d; // 0
d -= 3;
etch d; // -3

var (int) e = 3 % 3;
etch e; // 0
e %= 3;
etch e; // 0

var (arr) f = [1, 2, 3];
etch f@2; // 3

var (arr) g = 0..10;
etch f@2; // 3

var (arr) h = 0..b;
etch len(h); // 27

var (arr) i = (h@8)..19; // [8.000000, 9.000000, 10.000000, 11.000000, 12.000000, 13.000000, 14.000000, 15.000000, 16.000000, 17.000000, 18.000000]
etch i;

var (bool) j =  1 != 2;
etch j; // true

var (bool) k = 1 == 1;
etch k; // true

var (bool) l = 1 < 2;
etch l; // true

var (bool) m = 2 > 1;
etch m; // true

var (bool) n = 1 <= 2;
etch n; // true

var (bool) o = 2 >= 1;
etch o; // true

